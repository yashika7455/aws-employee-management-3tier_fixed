MongoDB for the employee management project.

Run from this folder:

`docker compose -f compose.yml up -d`

The container exposes MongoDB on `localhost:27017` and seeds a starter `emp`
database with demo admin/hr/employee accounts on first startup.
